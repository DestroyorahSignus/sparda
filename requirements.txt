# requirements.txt
# ── SPARDA's two standalone projects, reused as libraries (NOT vendored) ──
# Left UNVERSIONED for now: the v0.1.0 tags do not exist yet. Once DANTE and
# VERGIL are tagged, PIN these to @v0.1.0 for reproducibility (§2.1 / §10):
#   dante  @ git+https://github.com/DestroyorahSignus/dante.git@v0.1.0
#   vergil @ git+https://github.com/DestroyorahSignus/vergil.git@v0.1.0
dante  @ git+https://github.com/DestroyorahSignus/dante.git
vergil @ git+https://github.com/DestroyorahSignus/vergil.git
# (these transitively pull sentence-transformers, ragatouille, faiss, networkx,
#  cdlib, leidenalg, etc. — the explicit pins below are belt-and-suspenders so
#  SPARDA's eval/demo work even if a sub-dep loosens a version.)

# ── Core ML ──
torch>=2.2.0
transformers>=4.45.0
sentence-transformers>=5.0.0
faiss-cpu>=1.7.4

# ── Retrieval ──
rank-bm25>=0.2.2
ragatouille>=0.0.8

# ── Graph ──
networkx>=3.2
cdlib>=0.4.0
leidenalg>=0.10.0
python-igraph>=0.11.0
pyvis>=0.3.2

# ── LLM ──
llama-cpp-python>=0.2.70

# ── Data ──
datasets>=2.19.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# ── Utils ──
rapidfuzz>=3.6.0
gradio>=4.0.0
pyyaml>=6.0.0
tqdm>=4.66.0
