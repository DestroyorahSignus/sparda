# requirements.txt
# ── SPARDA's two standalone projects, reused as libraries ──
# For LOCAL / documented installs (pip install -e .) these are pinned to the
# released tags (§2.1 / §10). NOTE: the Modal images (modal_run.py / modal_demo.py)
# do NOT use these — they VENDOR the package source via add_local_dir from sibling
# clones (../dante-src, ../vergil-src), which avoids private-repo auth at build time.
dante  @ git+https://github.com/DestroyorahSignus/dante.git@v0.2.0
vergil @ git+https://github.com/DestroyorahSignus/vergil.git@v0.1.0
# (these transitively pull sentence-transformers, rerankers, faiss, networkx,
#  cdlib, leidenalg, etc. — the explicit pins below are belt-and-suspenders so
#  SPARDA's eval/demo work even if a sub-dep loosens a version.)

# ── Core ML (aligned to the DANTE/VERGIL-validated 4.x stack; the ColBERT reranker
#    via `rerankers` hooks transformers-4.x internals removed in 5.x, so DO NOT bump
#    transformers/sentence-transformers to 5.x here) ──
torch>=2.2.0
transformers>=4.51,<5
sentence-transformers>=4.1,<5
faiss-cpu>=1.7.4
scipy>=1.10               # SPLADE CSR-matmul scoring
scikit-learn>=1.3.0       # engine/global_search cosine_similarity

# ── Retrieval ──
rank-bm25>=0.2.2
rerankers[transformers]>=0.10.0   # ColBERT + cross-encoder rerank (answerai-colbert-small-v1)

# ── Graph ──
networkx>=3.2
cdlib>=0.4.0
leidenalg>=0.10.0
python-igraph>=0.11.0

# ── LLM ──
# Qwen3-4B-Instruct-2507 shared generator: transformers backend on GPU (Modal), or the
# llama.cpp GGUF backend on a Kaggle T4 (llama-cpp-python needed only for the GGUF route).
llama-cpp-python>=0.2.70

# ── Data ──
datasets>=2.19.0
pandas>=2.0.0
numpy>=1.24.0

# ── Demo / serving ──
fastapi>=0.110            # demo API host (static page + JSON/SSE, modal_demo.py)

# ── Utils ──
rapidfuzz>=3.6.0
pyyaml>=6.0.0
tqdm>=4.66.0
